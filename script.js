document.addEventListener('DOMContentLoaded', () => {
    // Simple typing effect for the profile summary
    // Simple typing effect removed as per request
    // The text will now render normally without animation

    // Add blink effect to cursors if we had them explicitly
    // This is just a placeholder for future dynamic interactions
    console.log("System initialized. Welcome, User.");
});
